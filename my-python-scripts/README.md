# my-python-scripts

Minimal workspace for small Python scripts used in class exercises.

Files
- `main.py` - sample entrypoint that prints a greeting and echoes arguments.
- `requirements.txt` - place to pin dependencies (empty by default).
- `.gitignore` - commonly ignored files for Python projects.

Usage

Run the sample script:

    python my-python-scripts/main.py hello world
